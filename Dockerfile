FROM node:13

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . /app

ARG PORT
ARG DB_USER
ARG DB_NAME
ARG DB_PASSWORD
ARG DB_HOST

ENV PORT=$PORT
ENV DB_HOST=$DB_HOST
ENV DB_NAME=$DB_NAME
ENV DB_USER=$DB_USER
ENV DB_PASSWORD=$DB_PASSWORD

EXPOSE $PORT

CMD ["npm", "start"]